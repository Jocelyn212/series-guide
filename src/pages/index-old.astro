---
import FringeCard from "../components/FringeCard";
import { getAnalisis, type Analisis } from "../lib/mongo";
import "../styles/global.css";

// Obtiene análisis desde MongoDB
const analisisData = await getAnalisis();
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Análisis de Series | SeriesGuide</title>
    <meta
      name="description"
      content="Análisis profesionales de las mejores series de TV. Fringe, Game of Thrones, Stranger Things y más."
    />
  </head>
  <body>
    {/* Navigation */}
    <nav class="bg-white shadow-md">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <a href="/" class="text-xl font-bold text-blue-600"
              >📺 SeriesGuide</a
            >
          </div>
          <div class="flex items-center space-x-4">
            <a href="/" class="text-blue-600 font-medium">Inicio</a>
            <a
              href="/series"
              class="text-gray-600 hover:text-blue-600 transition-colors"
              >Series</a
            >
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-4xl mx-auto p-4">
      <div class="text-center mb-8 mt-8">
        <h1 class="text-4xl font-bold text-blue-600 mb-4">
          Análisis Profesionales de Series
        </h1>
        <p class="text-gray-600 text-lg max-w-2xl mx-auto">
          Descubre análisis detallados, teorías fascinantes y interpretaciones
          profundas de las mejores series de televisión.
        </p>
        <a
          href="/series"
          class="inline-block mt-6 bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg font-medium transition-colors"
        >
          Ver Todas las Series
        </a>
      </div>

      <h2 class="text-2xl font-bold mb-6 text-gray-800">Últimos Análisis</h2>

      <div class="grid grid-cols-1 gap-6">
        {
          analisisData.map((item: Analisis) => (
            <FringeCard
              title={item.title}
              excerpt={item.excerpt}
              description={item.content}
              universe={item.universe || "blue"}
              author={item.author}
              readTime={item.readTime}
              views={item.views}
              likes={item.likes}
              publishedAt={item.publishedAt}
            />
          ))
        }
      </div>
    </main>
  </body>
</html>
